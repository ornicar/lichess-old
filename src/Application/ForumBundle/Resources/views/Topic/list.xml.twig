{% for topic in topics %}
    <entry>
        <title>{{ topic.subject }}</title>
        <author>{{ topic.authorName|default("Anonymous") }}</author>
        <updated>{{ topic.pulledAt|date("c")|raw }}</updated>
        <id>{{ forum_route_topic(topic, true)|raw }}</id>
        <link rel="alternate">{{ forum_route_topic(topic, true)|raw }}</link>
    </entry>
{% endfor %}
